server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  server_tokens off;

  add_header X-Content-Type-Options "nosniff" always;
  add_header Referrer-Policy "no-referrer" always;
  add_header X-Frame-Options "DENY" always;
  add_header Content-Security-Policy "default-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; object-src 'none'; frame-ancestors 'none'; base-uri 'none'" always;

  location = /status {
    default_type application/json;
    return 200 '{"message":"Hello, World!","runtime":"Runpod Secure Cloud","status":"healthy"}';
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
